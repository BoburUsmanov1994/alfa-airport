const config = {
    API_ROOT: process.env.REACT_APP_BASE_URL,
    FILE_URL: process.env.REACT_APP_FILE_URL,
    DEFAULT_APP_LANG: 'ru',
    ROLES: {
        observer: 'observer',
        operator: 'operator',
    },
    PERMISSIONS: [],
}

export default config;
